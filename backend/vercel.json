{
  "version": 2,
  "buildCommand": "npm install --include=dev && npx --yes tsc && npx prisma generate",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}

